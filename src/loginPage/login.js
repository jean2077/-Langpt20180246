import React from 'react';
import './login.css'; // 스타일 시트 임포트

const Login = () => {
    return (
        <div>
            <h2>로그인 페이지</h2>
            {/* 로그인 폼 등 추가 내용 */}
        </div>
    );
};

export default Login; // 기본 내보내기
